# Advanced ML HW 1

## Part 0 and 1

The files under `sanity_check/` are for part 0, with the related `inference_language.py`, `train_language.py` (part0 specific scripts). The files under `arithmetic_task/` are for part1, with the related `inference.py`, `train.py` scripts. The file `model.py` contains an implementation of a transformer model. The file `part_0_1_contract.py` contains some function signatures that would make autograding less painful for the TAs. 

## Part 2

The notebook `part_2_starter.ipynb` has code to load pretrained models, the AIME dataset, functions for evaluation and has all of our work to conduct the part 2 experiments. The files under `scaling_results/` are our results from the scaling experiments, with `run_inference.py` being our inference script for part 2.


## Writeup

The file `CSE493S_Homework_1_Writeup.pdf` contains our report for the homework.